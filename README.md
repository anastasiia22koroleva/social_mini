Социальная сеть мини-постов
Мини-приложение для обмена короткими записями с возможностью взаимодействия через лайки и комментарии (базовый функционал: регистрация, посты, JWT-аутентификация).

📋 Функционал
✅ Регистрация и аутентификация пользователей (JWT)
✅ Создание, чтение, редактирование и удаление постов (CRUD)
✅ Защита эндпоинтов — только авторизованные пользователи
✅ Пагинация ленты постов
✅ Работа с PostgreSQL через SQLAlchemy (асинхронно)
✅ Миграции базы данных (Alembic)
✅ Автоматическая документация API (Swagger UI)
✅ Тесты эндпоинтов (pytest + httpx)
✅ Готов к развёртыванию через Docker Compose

Технологии
Backend: FastAPI 0.115
База данных: PostgreSQL 16
ORM: SQLAlchemy 2.0 (асинхронный)
Аутентификация: JWT (python-jose)
Управление зависимостями: Poetry
Контейнеризация: Docker, Docker Compose
Тестирование: pytest, httpx

Быстрый запуск (Docker)
Проект полностью готов к запуску в изолированной среде:
# Сборка и запуск
docker-compose up --build

# Приложение будет доступно:
#   API:          http://localhost:8000
#   Документация: http://localhost:8000/docs

Локальный запуск (для разработки)
Требования
Python 3.12
Poetry
(Опционально) Локальный PostgreSQL или запуск через Docker (docker-compose up -d db)

# Установка зависимостей
poetry install

# Применение миграций (если БД уже запущена)
poetry run alembic upgrade head

# Запуск сервера разработки
poetry run uvicorn app.main:app --reload

Тестирование:
# Запуск тестов
poetry run pytest

Тесты проверяют:
Регистрацию и вход пользователя
Создание и удаление постов
Защиту эндпоинтов

Структура проекта

social_mini/
├── app/               # Исходный код приложения
│   ├── core/          # Безопасность, конфигурация
│   ├── api/           # Эндпоинты (auth, posts)
│   ├── models/        # Модели SQLAlchemy
│   ├── schemas/       # Pydantic-схемы
│   ├── crud/          # Операции с БД
│   └── database.py    # Подключение к БД
├── alembic/           # Миграции БД
├── tests/             # Тесты
├── pyproject.toml     # Зависимости (Poetry)
├── docker-compose.yml # Конфигурация развёртывания
├── Dockerfile         # Сборка образа
└── README.md

Зависимости:
Проект использует Poetry для управления зависимостями.

Примеры запросов:

1. Регистрация
POST /auth/register
Content-Type: application/json

{
  "username": "ivan",
  "email": "ivan@example.com",
  "password": "secret123",
  "first_name": "Иван",
  "last_name": "Иванов"
}

2. Получение токена

POST /auth/token
Content-Type: application/x-www-form-urlencoded

username=ivan&password=secret123

3. Создание поста (требуется токен)

POST /posts/
Authorization: Bearer <ваш_jwt_токен>
Content-Type: application/json

{
  "title": "Привет!",
  "content": "Мой первый пост"
}

Сборка и развёртывание:
docker-compose up --build

Авторы:
Студенты Московского физико-технического института (МФТИ), направления разработка It продукта:
Чепурных Артур,Алена Рагозина,Виктор Смирнов,Илья Мельников,Георгий Сердюков.
Курс: «Язык Python для разработчиков»